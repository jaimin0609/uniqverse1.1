"use client";

// Reuse the same component as the edit page, but without an ID
import SupplierEditPage from "../[id]/page";

export default function AddSupplierPage() {
    return <SupplierEditPage />;
}